<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @post.user.name.blank? ? @post.user.email : @post.user.name %>
</p>

<p>
  <strong>Content:</strong>
  <% if can? :crud, @post %>
    <%= @post.content %>
</p>

<% if can? :crud, @post %>
   <%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
