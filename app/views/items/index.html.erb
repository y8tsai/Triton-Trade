<head>
	<style>
		.products li { display: inline-block; width: 400px; vertical-align:top; }
		.products h1, h4 { font-family: "Oswald", sans-serif; text-align: center; }
		.products p { text-align: center; }
	</style>
</head>

<%= render 'shared/nav_bar' %>
<center><h1>For Sale</h1></center>

<% if user_signed_in? %>
  <center><%= link_to new_item_path, :method => :get do %>
		<button>New Item</button>
	  <% end %>
  </center>
<% end %>

<ul class="products">
<% @items.each do |item| %>
	<li>
		<!--<p><%= item.image_url %></p>-->
		<p><%= image_tag item.image_url.blank? ? ("http://www.drivenmavens.com/wp-content/uploads/Blank-page.jpg") : item.image_url, :width => 400, :height => 400, :crop => :fill %></p>
		<h4><%= item.name %></h4>
		<p>$ <%= item.price %></p>
		<p><%= link_to 'Details', item %></p>
		<p><%= raw tag_links(item.all_tags) %></p>
	</li>
<% end %>
</ul>

<!--<table>
  <thead>
    <tr>
      <th style="padding-right: 10px">User</th>
      <th style="padding-right: 10px">Item</th>
      <th style="padding-right: 10px">Condition</th>
      <th style="padding-right: 10px">Price</th> 
      <th style="padding-right: 10px">Tags</th> 
      <th colspan="30"></th>
    </tr>
  </thead>-->

<!--  <tbody>
    <% @items.each do |item| %>
      
      <tr>
        <td><%= item.user.name.blank? ? item.user.email : item.user.name %></td>
        <td style="padding-right: 10px"><%= item.name %></td>
	<td style="padding-right: 10px"><%= item.condition %></td>
	<td style="padding-right: 10px"><%= item.price %></td>
        <td style="padding-right: 10px"><%= raw tag_links(item.all_tags) %></td>
        <td style="padding-right: 10px"><%= link_to 'Details', item %></td>
        <% if user_signed_in? %>
           <% if can? :crud, item %>
              <td style="padding-right: 10px"><%= link_to 'Edit', edit_item_path(item) %></td>
              <td style="padding-right: 10px"><%= link_to 'Delete', item, method: :delete %></td>
           <% end %>
        <% end %>

      </tr>
    <% end %>
  </tbody>
</table>-->

<br>

